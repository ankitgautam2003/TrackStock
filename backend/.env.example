# MongoDB Configuration
# Option 1: Local MongoDB (default)
MONGODB_URI=mongodb://localhost:27017/inventory-db

# Option 2: MongoDB Atlas (cloud - recommended if local MongoDB not installed)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/inventory-db

# Server Configuration
PORT=5000
NODE_ENV=development

# CORS Configuration
CORS_ORIGIN=http://localhost:3000
